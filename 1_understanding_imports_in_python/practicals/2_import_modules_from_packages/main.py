from package_1.module_1 import function_package
# can import modules inside packages jst like importing modules in same folder as 'main.py'
# Knowing this could make it easier to organise code into different files and folders

function_package()