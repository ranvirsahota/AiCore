import module_1
import module_2

# when there is a '__main__.py' file present can execute file like so: 'python .'
# '.' can be used in arguments to specify current directoy
# '__main__.py' is ran by default and is the main file that is typically starting point for most executions of programs so python looks for files by this by default